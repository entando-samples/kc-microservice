server:
  port: 8081
  error:
    whitelabel:
      enabled: true
    path: /error

logging:
  level:
    ROOT: DEBUG
    org:
      springframework:
        security: debug

#https://medium.com/devops-dudes/securing-spring-boot-rest-apis-with-keycloak-1d760b2004e
keycloak:
  auth-server-url: http://quickstart-kc-entando.192.168.64.2.nip.io/auth
  bearer-only: true
  credentials:
    secret: a430df05-8ba3-4e3c-bef1-4c508a6ee564
  realm: entando
  resource: germanogiudici-entando-bundle-se-server
  use-resource-role-mappings: true

app:
  version: 1
  name: springboot-kc-sample
  security:
    auth-server-url: ${keycloak.auth-server-url}
    realm: ${keycloak.realm}
    client-ui: entando-web
